# ============================================
# ICT Agent API - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ============================================
# REQUIRED - API Keys
# ============================================

# OpenAI API Key (REQUIRED)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# ============================================
# OPTIONAL - External Services
# ============================================

# Pinecone API Key (Optional - for vector database search)
# Get your key from: https://app.pinecone.io/
PINECONE_API_KEY=your_pinecone_api_key_here

# Pinecone Index Name (Optional - default: default-index)
PINECONE_INDEX_NAME=default-index

# MCP RAG Sheets URL (Optional - default: https://www.ictpk.cloud/mcp/rag-sheets)
MCP_RAG_SHEETS_URL=https://www.ictpk.cloud/mcp/rag-sheets

# ============================================
# AI Model Configuration
# ============================================

# Model name (default: gpt-4o)
# Options: gpt-4o, gpt-4, gpt-3.5-turbo
MODEL_NAME=gpt-4o

# Temperature for LLM (default: 0.7)
# Range: 0.0 (deterministic) to 2.0 (creative)
TEMPERATURE=0.7

# ============================================
# Memory & Storage Configuration
# ============================================

# Path to store ChromaDB database (default: ./memory_db)
MEMORY_DB_PATH=./memory_db

# Number of turns before summarizing conversation (default: 10)
SUMMARIZE_INTERVAL=10

# ============================================
# API Server Configuration
# ============================================

# API server host (default: 0.0.0.0)
# Use 0.0.0.0 to accept connections from all interfaces
API_HOST=0.0.0.0

# API server port (default: 8009)
API_PORT=8009

# Environment mode (default: development)
# Options: development, production
ENVIRONMENT=development

# Enable auto-reload for development (default: false)
# Set to true for development, false for production
API_RELOAD=false

# ============================================
# Production Performance Settings
# ============================================

# Number of worker processes (default: 1, production: 4)
# Increase for better performance (recommended: 4 for production)
WORKERS=4

# Maximum concurrent connections (default: 100)
MAX_CONCURRENT=100

# Keep-alive timeout in seconds (default: 120)
# Increase for long-running requests
KEEP_ALIVE=120

# Request timeout in seconds (default: 120)
# Increase for long-running AI processing
TIMEOUT=120

# ============================================
# Logging Configuration
# ============================================

# Log level (default: info)
# Options: debug, info, warning, error, critical
LOG_LEVEL=info

# ============================================
# CORS & Security Configuration
# ============================================

# CORS allowed origins (default: *)
# For production, specify exact origins: https://your-domain.com,https://www.your-domain.com
# Use * for development only
CORS_ORIGINS=*

# Trusted hosts for proxy security (default: *)
# For production, specify exact hosts: api.your-domain.com,www.your-domain.com
# Use * to accept all hosts (development only)
TRUSTED_HOSTS=*

# ============================================
# Nginx Proxy Configuration
# ============================================

# Root path for subpath proxying (default: empty)
# If nginx proxies to /api/, set this to /api
# Leave empty if proxying to root (/)
ROOT_PATH=

# ============================================
# Example Production Configuration
# ============================================
# Uncomment and customize for production:

# ENVIRONMENT=production
# API_RELOAD=false
# WORKERS=4
# MAX_CONCURRENT=100
# KEEP_ALIVE=120
# TIMEOUT=120
# LOG_LEVEL=info
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com
# TRUSTED_HOSTS=api.your-domain.com,www.your-domain.com
# ROOT_PATH=/api
